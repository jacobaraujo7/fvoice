{
  "tasks": [
    {
      "label": "Run",
      "command": "sh",
      "args": ["-c", "xcodegen generate && xcodebuild -scheme FVoice -configuration Debug -derivedDataPath build build && { pkill -x FVoice; sleep 0.5; open build/Build/Products/Debug/FVoice.app; }"],
      "kind": "oneShot"
    },
    {
      "label": "Release",
      "command": "sh",
      "args": ["-c", "xcodegen generate && xcodebuild -scheme FVoice -configuration Release -derivedDataPath build build && { pkill -x FVoice; sleep 0.5; rm -rf /Applications/FVoice.app && ditto build/Build/Products/Release/FVoice.app /Applications/FVoice.app && open /Applications/FVoice.app; }"],
      "kind": "oneShot"
    }
  ]
}
